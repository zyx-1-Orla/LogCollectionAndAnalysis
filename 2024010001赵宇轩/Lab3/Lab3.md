# 密码学 习题集 - 流密码

---

## 第 1 题（1 分）

数据压缩常用于数据存储和传输。假设你想将数据压缩与加密结合使用，以下哪种顺序更合理？

- [ ] 先压缩，再加密
- [ ] 顺序无所谓——两者都不会压缩数据
- [ ×] 先加密，再压缩
- [ ] 顺序无所谓——两种顺序都可以

---

## 第 2 题（1 分）

设 $G: \{0,1\}^s \to \{0,1\}^n$ 是一个安全的伪随机数生成器（PRG）。以下哪些也是安全的 PRG？（可能有多个正确答案）

- [ ] $G'(k) = G(0)$
- [×] $G'(k) = G(k \oplus 1^s)$
- [ ] $G'(k) = G(k) \| G(k)$（其中 $\|$ 表示拼接）
- [×] $G'(k) = G(k)[0, \ldots, n-2]$（即丢弃 $G(k)$ 的最后一位）
- [ ] $G'(k) = G(k) \| 0$（其中 $\|$ 表示拼接）
- [×] $G'(k) = G(k) \oplus 1^n$

---

## 第 3 题（1 分）

设 $G: K \to \{0,1\}^n$ 是一个安全的 PRG。

定义 $G'(k_1, k_2) = G(k_1) \wedge G(k_2)$，其中 $\wedge$ 为逐位与（AND）运算。

考虑如下对 $\{0,1\}^n$ 的统计测试 $A$：

$$A(x) \text{ 输出 } \mathrm{LSB}(x)$$

即输出 $x$ 的最低有效位。

请问 $\mathrm{Adv}_{\mathrm{PRG}}[A, G']$ 是多少？

> 假设恰好有一半的种子 $k \in K$ 使得 $\mathrm{LSB}(G(k)) = 0$。

**注意**：请将优势以 0 到 1 之间的小数输入，小数点前须有 0。例如优势为 3/4，则输入 0.75。

**答案：** _0.25__________

---

## 第 4 题（1 分）

设 $(E, D)$ 是一个（一次性）语义安全的密码，密钥空间为 $K = \{0,1\}^\ell$。一家银行希望将解密密钥 $k \in \{0,1\}^\ell$ 拆分为两份 $p_1$ 和 $p_2$，使得只有同时持有两份才能解密。

**背景说明（两份拆分）：**  
银行随机生成 $k_1 \in \{0,1\}^\ell$，并令 $k_1' \leftarrow k \oplus k_1$，则 $k_1 \oplus k_1' = k$。将 $k_1$ 交给一位高管，$k_1'$ 交给另一位，两人必须同时在场才能解密（单独持有任意一份都不含关于 $k$ 的任何信息）。

**本题（三份拆分）：**  
现在银行希望将 $k$ 拆分为三份 $p_1, p_2, p_3$，使得**任意两份**都足以解密。这样即使一位高管缺席，解密仍可进行。

为此，银行生成两对随机密钥 $(k_1, k_1')$ 和 $(k_2, k_2')$，满足：

$$k_1 \oplus k_1' = k_2 \oplus k_2' = k$$

请问银行应如何分配这些份额，使得任意两份可以恢复 $k$，但单独任意一份不能？

- [ ] $p_1 = (k_1,\, k_2),\quad p_2 = (k_1'),\quad p_3 = (k_2')$
- [ ] $p_1 = (k_1,\, k_2),\quad p_2 = (k_1,\, k_2),\quad p_3 = (k_2')$
- [ ] $p_1 = (k_1,\, k_2),\quad p_2 = (k_1',\, k_2'),\quad p_3 = (k_2')$
- [×] $p_1 = (k_1,\, k_2),\quad p_2 = (k_1',\, k_2),\quad p_3 = (k_2')$
- [ ] $p_1 = (k_1,\, k_2),\quad p_2 = (k_2,\, k_2'),\quad p_3 = (k_2')$

---

## 第 5 题（1 分）

设 $M = C = K = \{0, 1, 2, \ldots, 255\}$，考虑如下定义在 $(K, M, C)$ 上的密码：

$$E(k, m) = m + k \pmod{256}$$
$$D(k, c) = c - k \pmod{256}$$

该密码是否具有完美保密性（perfect secrecy）？

- [× ] 是的。
- [ ] 不是，只有一次性密码本（OTP）具有完美保密性。
- [ ] 不是，该密码存在简单的攻击方式。

---

## 第 6 题（1 分）

设 $(E, D)$ 是一个（一次性）语义安全的密码，消息空间和密文空间均为 $\{0,1\}^n$。以下哪些加密方案是（一次性）语义安全的？

- [ ] $E'(k, m) = E(k, m) \| \mathrm{LSB}(m)$
- [ ×] $E'(k, m) = \mathrm{reverse}(E(k, m))$（翻转密文的比特顺序）
- [ ] $E'(k, m) = E(k, m) \| k$
- [× ] $E'(k, m) = 0 \| E(k, m)$（在密文前添加比特 0）
- [ ×] $E'((k, k'),\, m) = E(k, m) \| E(k', m)$
- [ ] $E'(k, m) = E(0^n, m)$

---

## 第 7 题（1 分）

假设你知道消息 "attack at dawn" 的一次性密码本（OTP）加密结果为：

```
6c73d5240a948c86981bc294814d
```

（明文字母用 8 位 ASCII 编码，密文以十六进制表示）

请问在**相同 OTP 密钥**下，消息 "attack at dusk" 的加密结果是什么？

**答案：** _6c73d5240a948c86981bc297804a

---

## 第 8 题（1 分）

电影行业希望保护 DVD 上分发的数字内容，以下描述了一种类似于蓝光盘保护方案 AACS 的变体。

**系统设定：**  
假设全球最多有 $n$ 台 DVD 播放器（例如 $n = 2^{32}$）。将这 $n$ 台播放器视为一棵高度为 $\log_2 n$ 的**二叉树**的叶节点。树中每个节点包含一个 AES 密钥 $k_i$，这些密钥对消费者保密且永久固定。

制造时，每台播放器被分配序号 $i \in [0, n-1]$，并被嵌入从根节点到叶节点 $i$ 路径 $S_i$ 上所有节点对应的密钥。

一部 DVD 电影 $m$ 被加密为：

$$E(k_{\text{root}},\, k) \;\|\; E(k, m)$$

其中 $k$ 是随机生成的内容密钥，$k_{\text{root}}$ 是根节点密钥。由于所有播放器都有 $k_{\text{root}}$，所有播放器均可解密电影。

**密钥撤销：**  
假设播放器 $r$ 的密钥被黑客窃取并公开于互联网。在发行新 DVD 时，电影行业可以使用一个稍大的包头（约含 $\log_2 n$ 个密文），使得**除播放器 $r$ 之外**的所有播放器均可解密该电影，从而"禁用"播放器 $r$。

**本题：**  
如下图所示，考虑一棵有 $n = 16$ 个叶节点的树。假设标号为 **25** 的叶节点对应于被暴露的播放器密钥。请选择应在哪些节点的密钥下加密内容密钥 $k$，使得除播放器 25 以外的所有播放器均可解密 DVD。

> 📌 **注意**：需要恰好 **4 个**密钥。

![image-20260329173740252](./assets/image-20260329173740252.png)

- [] 9
- [× ] 1
- [ ×] 28
- [×] 6
- [ ] 20
- [ ] 19
- [ ] 26
- [× ] 11

---

## 第 9 题（1 分）

接续第 8 题，若共有 $n$ 台 DVD 播放器，当恰好**一台**播放器的密钥需要被撤销时，内容密钥 $k$ 需要在多少个密钥下进行加密？

- [ ] $\sqrt{n}$
- [ ] $n/2$
- [ ] $n - 1$
- [ ] $2$
- [ ×] $\log_2 n$

---

## 第 10 题（1 分）

接续第 8 题，假设标号为 **16、18、25** 的叶节点对应于被暴露的播放器密钥。请选择最小的密钥集合，在这些密钥下加密内容密钥 $k$，使得除播放器 16、18、25 以外的所有播放器均可解密 DVD。

> 📌 **注意**：需要恰好 **6 个**密钥。

- [×] 4
- [ ] 6
- [ ×] 11
- [ ×] 15
- [× ] 17
- [ ×] 26
- [ ] 12
- [ ] 9
- [ ×] 28
- [ ] 19

---

## 第 11 题（1 分）

假设字母表包含 26 个字母，替换密码（Substitution Cipher）的密钥空间大小是多少？

- [ ] $|\mathcal{K}| = 26$
- [×] $|\mathcal{K}| = 26!$（26 的阶乘）
- [ ] $|\mathcal{K}| = 2^{26}$
- [ ] $|\mathcal{K}| = 26^2$

---

## 第 12 题（1 分）

在英文文本中，出现频率最高的字母是哪个？

- [ ] "X"
- [ ] "L"
- [ ×] "E"
- [ ] "H"

---

## 第 13 题（1 分）

**复习：异或（XOR）运算**

$\{0,1\}^n$ 中两个字符串的异或（XOR）是它们的逐位模 2 加法。

请计算：$0110111 \oplus 1011010 = ?$

**答案：** ___1101101________

---

## 第 14 题（1 分）

**异或的一个重要性质**

**定理**：设 $Y$ 是 $\{0,1\}^n$ 上的随机变量，$X$ 是 $\{0,1\}^n$ 上**独立的**均匀随机变量。则 $Z := Y \oplus X$ 在 $\{0,1\}^n$ 上是均匀分布的。

**问题**：该定理说明了什么？

- [ ] $Y \oplus X$ 的分布取决于 $Y$ 的分布
- [ ×] 任何随机变量与独立的均匀随机变量异或后，结果仍是均匀分布
- [ ] $X$ 和 $Y$ 必须同分布
- [ ] 异或运算不改变分布

---

## 第 15 题（1 分）

给定一条消息 $m$ 及其 OTP（一次性密码本）加密后的密文 $c$。  
你能从 $m$ 和 $c$ 计算出 OTP 密钥吗？

- [ ] 不能，我无法计算出密钥。
- [×] 能，密钥是 $k = m \oplus c$。
- [ ] 我只能计算出密钥的一半比特。
- [ ] 能，密钥是 $k = m \oplus m$。

---

## 第 16 题（1 分）

设 $m \in \mathcal{M}$，$c \in \mathcal{C}$。  
有多少个 OTP 密钥可以将 $m$ 映射到 $c$？

- [ ] 无
- [× ] 1
- [ ] 2
- [ ] 取决于 $m$

---

## 第 17 题（1 分）

流密码（Stream Cipher）能否具有完美保密性（Perfect Secrecy）？

- [ ] 是的，如果 PRG 真的是"安全"的
- [ ] 不能，不存在具有完美保密性的密码
- [ ] 是的，每个密码都具有完美保密性
- [ ×] 不能，因为密钥比消息短

---

## 第 18 题（1 分）

假设 $G: K \to \{0,1\}^n$ 满足对所有 $k$ 都有：$\text{XOR}(G(k)) = 1$（即 $G(k)$ 所有比特的异或结果为 1）。

问：$G$ 是否可预测？

- [ ] 是的，给定第一个比特我可以预测第二个
- [ ] 不可预测，$G$ 是不可预测的
- [× ] 是的，给定前 $(n-1)$ 个比特我可以预测第 $n$ 个比特
- [ ] 视情况而定

---

## 第 19 题（1 分）

设 $G: K \to \{0,1\}^n$ 是一个 PRG，满足：从 $G(k)$ 的后 $n/2$ 个比特可以很容易地计算出前 $n/2$ 个比特。

问：$G$ 对于某个 $i \in \{0, \ldots, n-1\}$ 是否可预测？

- [ ×] 是
- [ ] 否

---

## 第 20 题（问答/证明题，3 分）

**PRG 优势（Advantage）的定义与计算**

设 $G: K \to \{0,1\}^n$ 是一个 PRG，$A$ 是 $\{0,1\}^n$ 上的统计测试。

**(a)** 请写出 PRG 优势 $\mathrm{Adv}_{\mathrm{PRG}}[A, G]$ 的定义式。

**答案：Adv_{PRG}[A, G] = | Pr_{k <- K}[A(G(k)) = 1] - Pr_{x <- 0, 1^n}[A(x) = 1] |


```

**(b)** 解释 $\mathrm{Adv}_{\mathrm{PRG}}[A, G]$ 接近 1 和接近 0 分别意味着什么。

**答案：### 含义解释
- 当 $\mathrm{Adv}_{\mathrm{PRG}}[A,G]$ **接近1**时：
  算法$A$能高效区分伪随机串与真随机串，$G$伪随机性差、安全性弱。
- 当 $\mathrm{Adv}_{\mathrm{PRG}}[A,G]$ **接近0**时：
  算法$A$无法区分伪随机串与真随机串，$G$伪随机性好、安全性强。

```

**(c)** 若 $A(x) = 0$（即 $A$ 对任何输入都输出 0），求 $\mathrm{Adv}_{\mathrm{PRG}}[A, G]$ 的值。

**答案：**###  计算过程
已知$A(x)=0$对任意输入成立，则：
$$
\Pr_{k\leftarrow K}[A(G(k))=1] = 0, \quad \Pr_{x\leftarrow \{0,1\}^n}[A(x)=1] = 0
$$

代入公式得：
$$
\mathrm{Adv}_{\mathrm{PRG}}[A,G] = |0 - 0| = 0
$$

**答案：$\mathrm{Adv}_{\mathrm{PRG}}[A,G] = 0$**

```

```

---

## 第 21 题（问答/证明题，4 分）

**PRG 优势的具体计算**

假设 $G: K \to \{0,1\}^n$ 满足：对于 $K$ 中 $\frac{2}{3}$ 的密钥 $k$，有 $\mathrm{MSB}(G(k)) = 1$（即输出的最高有效位为 1）。

定义统计测试 $A(x)$ 如下：
$$A(x) = \begin{cases} 1 & \text{若 } \mathrm{MSB}(x) = 1 \\ 0 & \text{否则} \end{cases}$$

**(a)** 计算 $\Pr[A(G(k)) = 1]$，其中 $k \xleftarrow{R} K$。

**答案：**### 计算过程
已知统计测试 \( A(x) \) 的定义：
\[
A(x) = 
\begin{cases} 
1 & \text{若 } \text{MSB}(x) = 1 \\
0 & \text{否则}
\end{cases}
\]

事件 \( A(G(k)) = 1 \) 等价于 \( \text{MSB}(G(k)) = 1 \)。

题目中给出：对于密钥空间 \( K \) 中 \( \frac{2}{3} \) 的密钥 \( k \)，满足 \( \text{MSB}(G(k)) = 1 \)。

因此：
\[
\Pr_{k \leftarrow K}[A(G(k)) = 1] = \Pr_{k \leftarrow K}[\text{MSB}(G(k)) = 1] = \frac{2}{3}
\]

**答案：$\frac{2}{3}$**

```

```

**(b)** 计算 $\Pr[A(r) = 1]$，其中 $r \xleftarrow{R} \{0,1\}^n$。

**答案：**### 计算过程
已知统计测试 \( A(x) \) 的定义：
\[
A(x) = 
\begin{cases} 
1 & \text{若 } \text{MSB}(x) = 1 \\
0 & \text{否则}
\end{cases}
\]

\( r \) 是从 \( \{0,1\}^n \) 中均匀随机选取的字符串，其最高有效位 \( \text{MSB}(r) \) 只能取0或1，且两种取值的概率相等。

事件 \( A(r) = 1 \) 等价于 \( \text{MSB}(r) = 1 \)，因此：
\[
\Pr_{r \leftarrow \{0,1\}^n}[A(r) = 1] = \Pr_{r \leftarrow \{0,1\}^n}[\text{MSB}(r) = 1] = \frac{1}{2}
\]

**答案：$\frac{1}{2}$**

```

```

**(c)** 计算 $\mathrm{Adv}_{\mathrm{PRG}}[A, G]$。

**答案：**### 计算过程
根据PRG（伪随机生成器）优势的标准定义：
$$
\text{Adv}_{\text{PRG}}[A, G] = \left| \Pr_{k \leftarrow K}[A(G(k)) = 1] - \Pr_{r \leftarrow \{0,1\}^n}[A(r) = 1] \right|
$$

将(a)、(b)的计算结果代入公式：
$$
\text{Adv}_{\text{PRG}}[A, G] = \left| \frac{2}{3} - \frac{1}{2} \right|
$$

通分计算：
$$
\frac{2}{3} - \frac{1}{2} = \frac{4}{6} - \frac{3}{6} = \frac{1}{6}
$$

取绝对值后得到最终结果：
$$
\text{Adv}_{\text{PRG}}[A, G] = \frac{1}{6}
$$

**答案：$\frac{1}{6}$**

```

```

**(d)** 该 PRG 是否安全？为什么？

**答案：**### 分析过程
根据伪随机生成器（PRG）的安全定义：
一个PRG是安全的，当且仅当对于任意多项式时间的区分器\( A \)，其优势
$$
\text{Adv}_{\text{PRG}}[A, G] = \left| \Pr_{k \leftarrow K}[A(G(k)) = 1] - \Pr_{r \leftarrow \{0,1\}^n}[A(r) = 1] \right|
$$
是**可忽略的**（即随着安全参数增大，优势趋近于0）。

---

在本题中，我们构造的区分器\( A \)满足：
$$
\text{Adv}_{\text{PRG}}[A, G] = \frac{1}{6}
$$
该优势是一个**常数**，不会随安全参数的增大而减小，不属于可忽略的范畴。

因此，该PRG**不安全**✅。

```

```

---

## 第 22 题（证明题，5 分）

**证明 OTP 具有完美保密性**

**引理**：一次性密码本（OTP）具有完美保密性（Perfect Secrecy）。

**(a)** 写出完美保密性的定义：对于任意明文 $m$ 和密文 $c$，$\Pr_k[E(k,m) = c]$ 应满足什么条件？

**答案：**完美保密性的定义

完美保密性的定义为：
对于任意明文  m \in \mathcal{M}  和密文  c \in \mathcal{C} ，有：

\Pr\left[ E(k, m) = c \right] = \Pr[c]

即加密得到密文  c  的概率与明文  m  无关，仅由密文本身的分布决定。✅
OTP的完美保密性证明

已知OTP的加密规则为： c = m \oplus k （其中  k  是与  m  等长的一次性随机密钥，且密钥空间  \mathcal{K} 、明文空间  \mathcal{M} 、密文空间  \mathcal{C}  的大小相同）。

1. 密钥  k  是均匀随机选取的，即对任意  k \in \mathcal{K} ， \Pr[k] = \frac{1}{|\mathcal{K}|} 。

2. 对任意明文  m  和密文  c ，存在唯一的密钥  k = m \oplus c  使得  E(k, m) = c 。

3. 因此：

\Pr\left[ E(k, m) = c \right] = \Pr\left[ k = m \oplus c \right] = \frac{1}{|\mathcal{K}|}

4. 由于密文  c  是由均匀随机的密钥与明文异或得到，所以  \Pr[c] = \frac{1}{|\mathcal{C}|} ，而  |\mathcal{K}| = |\mathcal{C}| ，故：

\Pr\left[ E(k, m) = c \right] = \Pr[c]

满足完美保密性的定义，因此OTP具有完美保密性。✅
最终答案：
完美保密性定义为对任意明文 m 和密文 c ，\Pr\left[ E(k, m) = c \right] = \Pr[c] ；OTP满足该定义，故具有完美保密性。

```

```

**(b)** 对于 OTP，证明对任意 $m \in \mathcal{M}$ 和 $c \in \mathcal{C}$，恰好存在多少个密钥 $k$ 使得 $E(k, m) = c$？

**答案：**证明：恰好存在一个密钥 k 使得 E(k,m)=c
OTP的加密规则为：
c = E(k,m) = m \oplus k
（\oplus 表示按位异或运算）

对等式变形可得：
k = m \oplus c
对于任意固定的明文 m \in \mathcal{M} 和密文 c \in \mathcal{C}，m \oplus c 的结果是唯一的，因此恰好存在1个密钥 k 使得 E(k,m)=c。✅
对任意 m \in \mathcal{M} 和 c \in \mathcal{C}，恰好存在1个密钥 k=m \oplus c 使得 E(k,m)=c；

```

```

**(c)** 利用 (a) 和 (b) 的结论，完成 OTP 具有完美保密性的证明。

**答案：**利用(a)(b)完成OTP完美保密性的证明

1. 密钥空间为 \mathcal{K}，密钥 k 从 \mathcal{K} 中均匀随机选取，因此每个密钥被选中的概率为：
\Pr[k] = \frac{1}{|\mathcal{K}|}
2. 由(b)的结论可知：对任意 m \in \mathcal{M} 和 c \in \mathcal{C}，满足 E(k,m)=c 的密钥恰好有1个，因此：
\Pr\left[E(k,m)=c\right] = \frac{1}{|\mathcal{K}|}
3. 在OTP中，密文空间 \mathcal{C} 与密钥空间 \mathcal{K} 大小相等（|\mathcal{C}|=|\mathcal{K}|），且明文服从均匀分布，因此密文 c 的先验概率为：
\Pr[c] = \frac{1}{|\mathcal{C}|} = \frac{1}{|\mathcal{K}|}
4. 综上可得：
\Pr\left[E(k,m)=c\right] = \Pr[c]
完全满足(a)中完美保密性的定义，因此OTP具有完美保密性。✅
结合密钥均匀分布与(b)的结论，可证 \Pr\left[E(k,m)=c\right]=\Pr[c]，满足完美保密性定义，故OTP具有完美保密性。

```

```

---

## 第 23 题（证明题，5 分）

**语义安全性与明文信息泄露**

假设存在一个高效算法 $A$，它总是能从密文 $c$ 中推断出明文 $m$ 的最低有效位 $\mathrm{LSB}(m)$。

**(a)** 请证明：加密方案 $\mathbb{E} = (E, D)$ 不是语义安全的。

**答案：**证明加密方案 E=(E,D) 不是语义安全的

1. 语义安全的核心定义

一个加密方案是语义安全的，当且仅当不存在多项式时间的算法，能从密文中推断出明文的任何非平凡有效信息。

2. 已知条件

存在一个高效算法 A，它总能从密文 c 中推断出明文 m 的最低有效位 \operatorname{LSB}(m)。

3. 推理过程

• 明文的最低有效位 \operatorname{LSB}(m) 是明文的非平凡有效信息（它属于明文的确定特征，且能被算法 A 有效提取）。

• 算法 A 是高效的（多项式时间），且能从密文 c 中得到明文的确定信息 \operatorname{LSB}(m)。

• 这直接违反了语义安全的定义，因此加密方案 E=(E,D) 不是语义安全的。✅
最终答案：
因为存在高效算法 A 能从密文 c 中推断出明文 m 的最低有效位 \operatorname{LSB}(m)，而 \operatorname{LSB}(m) 是明文的非平凡有效信息，违反语义安全的定义，所以加密方案 E=(E,D) 不是语义安全的。

```

```

**(b)** 构造一个攻击者 $B$，使用 $A$ 作为子程序来赢得语义安全游戏。具体地：
   - 攻击者 $B$ 应选择哪两条消息 $m_0, m_1$ 发送给挑战者？
   - 收到密文 $c$ 后，$B$ 如何利用 $A$ 来猜测 $b$？

**答案：**(b) 构造攻击者B，使用A作为子程序赢得语义安全游戏
#### 1. 选择挑战明文
攻击者B选择两条满足**最低有效位（LSB）不同**的明文：
- \( m_0 \)：满足 \( \text{LSB}(m_0) = 0 \)
- \( m_1 \)：满足 \( \text{LSB}(m_1) = 1 \)
将 \( m_0, m_1 \) 发送给挑战者。

#### 2. 利用算法A猜测挑战比特b
挑战者返回密文 \( c = E(k, m_b) \)（其中 \( b \in \{0,1\} \) 为随机挑战比特），B调用子程序A对密文c进行计算，得到明文 \( m_b \) 的最低有效位 \( \text{LSB}(m_b) \)：
- 若 \( \text{LSB}(m_b) = 0 \)，则B猜测 \( b' = 0 \)
- 若 \( \text{LSB}(m_b) = 1 \)，则B猜测 \( b' = 1 \)

---

```

```

**(c)** 计算 $\mathrm{Adv}_{\mathrm{SS}}[B, \mathbb{E}]$ 的值。

**答案：** 计算 \( \text{Adv}_{\text{SS}}[B, E] \) 的值
#### 1. 语义安全优势的定义
语义安全优势的定义为：
$$
\text{Adv}_{\text{SS}}[B, E] = \left| \Pr[b' = b] - \frac{1}{2} \right|
$$

#### 2. 计算过程
- 由于算法A总能100%正确从密文c中推断出明文 \( m_b \) 的最低有效位 \( \text{LSB}(m_b) \)，因此B的猜测 \( b' \) 与真实挑战比特b完全一致，即 \( \Pr[b' = b] = 1 \)
- 代入优势公式：
$$
\text{Adv}_{\text{SS}}[B, E] = \left| 1 - \frac{1}{2} \right| = \frac{1}{2}
$$

**答案：\( \text{Adv}_{\text{SS}}[B, E] = \frac{1}{2} \)**

```

```

---

## 第 24 题（证明题，5 分）

**证明 OTP 是语义安全的**

考虑语义安全游戏的两个实验：
- **EXP(0)**：挑战者加密 $m_0$，即 $c \leftarrow k \oplus m_0$
- **EXP(1)**：挑战者加密 $m_1$，即 $c \leftarrow k \oplus m_1$

其中 $k \xleftarrow{R} K$，$|m_0| = |m_1|$。

**(a)** 证明在 EXP(0) 中，密文 $c = k \oplus m_0$ 在 $\{0,1\}^n$ 上是均匀分布的。

**答案：**### (a) 证明过程
OTP的密钥 \( k \) 是从 \( \{0,1\}^n \) 中均匀随机选取的，即对任意 \( k \in \{0,1\}^n \)，有：
$$
\Pr[k] = \frac{1}{2^n}
$$

对于任意固定的 \( m_0 \in \{0,1\}^n \) 和任意目标密文 \( c^* \in \{0,1\}^n \)，令 \( k^* = c^* \oplus m_0 \)，则：
$$
\Pr[c = c^*] = \Pr[k \oplus m_0 = c^*] = \Pr[k = k^*] = \frac{1}{2^n}
$$

因此，\( c \) 在 \( \{0,1\}^n \) 上是均匀分布的。

```

```

**(b)** 证明在 EXP(1) 中，密文 $c = k \oplus m_1$ 在 $\{0,1\}^n$ 上也是均匀分布的。

**答案：**### (b) 证明过程
密钥 \( k \) 从 \( \{0,1\}^n \) 中均匀随机选取，即对任意 \( k \in \{0,1\}^n \)，\( \Pr[k] = \frac{1}{2^n} \)。

对于任意固定的 \( m_1 \in \{0,1\}^n \) 和任意目标密文 \( c^* \in \{0,1\}^n \)，令 \( k^* = c^* \oplus m_1 \)，则：
$$
\Pr[c = c^*] = \Pr[k \oplus m_1 = c^*] = \Pr[k = k^*] = \frac{1}{2^n}
$$

因此，\( c \) 在 \( \{0,1\}^n \) 上是均匀分布的。

```

```

**(c)** 由 (a) 和 (b) 说明 EXP(0) 和 EXP(1) 中密文的分布是相同的（identical distributions）。

**答案：**### (c) 结论
由(a)和(b)可知：
- EXP(0)中密文 \( c \) 的分布是 \( \{0,1\}^n \) 上的均匀分布；
- EXP(1)中密文 \( c \) 的分布也是 \( \{0,1\}^n \) 上的均匀分布。

因此，两个实验中的密文分布完全相同。

```

```

**(d)** 利用 (c) 的结论，证明对于任意攻击者 $A$：
$$\mathrm{Adv}_{\mathrm{SS}}[A, \mathrm{OTP}] = \left| \Pr[A(k \oplus m_0) = 1] - \Pr[A(k \oplus m_1) = 1] \right| = 0$$

**答案：**### (d) 证明过程
语义安全优势的定义为：
$$
\text{Adv}_{\text{SS}}[A, \text{OTP}] = \left| \Pr[A(k \oplus m_0) = 1] - \Pr[A(k \oplus m_1) = 1] \right|
$$

由(c)可知，\( k \oplus m_0 \) 和 \( k \oplus m_1 \) 的分布完全相同，因此对于任意攻击者 \( A \)，有：
$$
\Pr[A(k \oplus m_0) = 1] = \Pr[A(k \oplus m_1) = 1] = p
$$

代入优势公式得：
$$
\text{Adv}_{\text{SS}}[A, \text{OTP}] = |p - p| = 0
$$

因此，OTP是语义安全的。

```

```

---

